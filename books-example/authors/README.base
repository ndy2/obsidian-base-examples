formulas:
  books: file.backlinks.map(value.asFile().properties.title)
views:
  - type: table
    name: Table
    filters:
      and:
        - file.folder == "books-example/authors"
    order:
      - name
      - formula.books
    sort:
      - property: formula.books
        direction: DESC
